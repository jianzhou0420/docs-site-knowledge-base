# Blueprint

> Stub landing page — the root `/` redirects here via `mkdocs.yml` `redirects.redirect_maps`.

Write your project's "what is this and why" here. This is the first page a new visitor sees.
